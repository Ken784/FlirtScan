rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: tighten rules per feature requirements
    match /{document=**} {
      allow read, write: if false; // default deny
    }
  }
}
